"""Global pytask configuration for the corporate bankruptcy ML project.

The project is built as a reproducible Python pipeline. Individual task files
inside the ``tasks/`` directory define how raw data is validated, transformed,
modeled, evaluated, and summarized.

Run:
    pixi run build

or equivalently:
    pixi run pytask
"""